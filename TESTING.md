# 功能测试手册

按顺序走完本文 = 覆盖当前版本全部功能。每步给出操作和预期结果。

## 功能总览

| 模块 | 已实现功能 |
|---|---|
| 启动 | 全局命令 `xiumi` 一键进入；自动后台拉起 Edge；登录态自动检测 |
| 界面 | 双栏欢迎卡、命令速查、滚动流水、忙碌转圈（含耗时秒数）、忙碌时底栏 esc 提示 |
| 命令 | `/model` `/file` `/login` `/shot` `/help` 五命令 |
| 补全 | 输入 `/` 弹候选、前缀过滤、Tab/Shift+Tab 循环、Enter 确定 |
| 登录 | 弹官网浏览器、后台轮询、成功/超时/esc 取消三种回报、登录态长期保留 |
| 配置 | 模型/Key/地址同屏填写，保存即生效并持久化到 .env |
| Agent | 任务输入 → LLM 自动写文 + 排版 + 设标题 + 保存草稿，全程工具调用可视 |
| 插图 | 任务文本用 `[img:路径]` 标记图片位置，自动上传插入 |
| 中断 | esc 随时中断任务或取消登录等待 |
| 工具 | 浏览器操控 8 项 + 登录检测 + 编辑器操作 7 项，共 18 个工具 |

## 准备

```powershell
# 方式一：源码运行（项目根目录）
python main.py

# 方式二：pip 安装后任意目录
pip install git+https://github.com/ZMJJKK123-hub/xiumi-creator.git
xiumi
```

需要一个 OpenAI 兼容的模型服务（模型名、API Key、接口地址三项，测试阶段在 `/model` 里填）。

## 阶段一：首次启动与界面

- [ ] 1.1 启动后自动拉起 Edge（独立浏览器档案，不影响你日常用的 Edge；首次约 3~5 秒）
- [ ] 1.2 出现橙色圆角欢迎卡：左栏 Welcome back! 与模型名，右栏「命令与快捷键」速查 7 条
- [ ] 1.3 未配置模型时左栏显示「模型: 未配置」；已配置则显示模型名
- [ ] 1.4 已登录过秀米：流水区出现「已登录」；从未登录过：出现未登录提示，只引导 `/login` 和 `/model` 两个命令
- [ ] 1.5 底部输入框可直接打字，鼠标滚轮可翻阅流水区，无横向滚动条
- [ ] 1.6 拖动终端窗口宽度：欢迎卡实时跟随、始终铺满终端；拉到很窄（约 60 列内）自动切单栏简版，拉宽又变回双栏，无需重启
- [ ] 1.7 模型名任何宽度下完整显示，不出现 … 截断

## 阶段二：/model 配置模型

- [ ] 2.1 输入 `/model` 回车 → 弹出配置屏，三个输入框：模型名、API Key、接口地址
- [ ] 2.2 Tab 在三个输入框间切换焦点
- [ ] 2.3 填完三项按 Enter 保存 → 回到主屏，提示保存成功，欢迎卡模型名立即变成新模型
- [ ] 2.4 重启应用 → 欢迎卡仍显示刚配置的模型名（已持久化）
- [ ] 2.5 检查项目 `.env` 文件，三项配置已写入
- [ ] 2.6 故意填一个错误 Key 提交任务 → 得到明确的接口报错提示，应用不崩溃；重新 `/model` 改对即可

## 阶段三：斜杠命令自动补全

- [ ] 3.1 输入 `/`（不回车）→ 上方弹出全部 5 个命令候选
- [ ] 3.2 继续输入 `/f` → 候选过滤到只剩 `/file`
- [ ] 3.3 按 Tab → 选中项循环下移；Shift+Tab 反向循环
- [ ] 3.4 候选选中 `/help` 时按 Enter → 直接执行打开帮助浮层
- [ ] 3.5 候选选中 `/file` 时按 Enter → 输入框变成 `/file ` 并留一个空格，等你补路径
- [ ] 3.6 输入普通文字（不以 / 开头）→ 候选面板隐藏

## 阶段四：/login 登录

- [ ] 4.1 输入 `/login` 回车 → 浏览器窗口弹出秀米官网登录页，终端出现等待提示，转圈开始，底栏变为「esc 中断任务」
- [ ] 4.2 在浏览器里自己完成账号登录（密码或微信扫码均可；如遇滑块拖一下）
- [ ] 4.3 登录成功后 5 秒内 → 终端停止转圈，提示「✻ 登录成功，可以开始任务」
- [ ] 4.4 再次启动应用 → 无需重新登录，直接显示「已登录」
- [ ] 4.5 超时路径：`/login` 后放着不动 5 分钟 → 提示「⚠ 登录等待超时，可重新 /login」
- [ ] 4.6 取消路径：`/login` 后按 esc → 立即停止等待，出现一条「已中断」提示（仅一条），浏览器窗口收回，底栏清空
- [ ] 4.7 登录已被测过一次后重复 `/login` → 轮询立刻发现已登录并直接报成功

## 阶段五：基础命令

- [ ] 5.1 `/shot` → 截图保存到 `screenshots/`，终端回显完整路径
- [ ] 5.2 `/help` → 弹出帮助浮层，按任意键关闭
- [ ] 5.3 `/file examples/task.md` → 文件内容展开显示，随后作为任务提交给 Agent
- [ ] 5.4 `/file`（不带路径）→ 提示「用法: /file 路径/到/task.md」
- [ ] 5.5 `/file 不存在.md` → 红色错误行「文件不存在」，应用不崩溃

## 阶段六：Agent 任务（核心流程）

前置：阶段二模型已配好、阶段四已登录。

- [ ] 6.1 输入任务「写一篇秋天咖啡店探店推文，主色暖棕」回车
- [ ] 6.2 灰底命令条回显任务 → 转圈开始，底栏「esc 中断任务」
- [ ] 6.3 流水区依次出现思考文字和工具行，格式 `└ 工具名(参数)`，结果灰字折叠显示
- [ ] 6.4 任务全程浏览器保持后台不弹出；完成后打开浏览器核对：新建图文 → 标题写入 → 排版内容 → 已保存
- [ ] 6.5 任务完成 → 汇总文字 + 「✻ 共 N 次工具调用，用时 Ns」，转圈停止，底栏清空
- [ ] 6.6 到秀米官网「我的图文」里核对：草稿存在，标题、正文块、配色与任务要求一致
- [ ] 6.7 排版规范抽查：内容为内联样式 HTML，无 div、无外链 CSS，移动端预览不错位

带图片的任务（准备一张本地图片，如 `D:\pics\test.jpg`）：

- [ ] 6.8 输入任务「写一篇两段式的秋天问候短文，开头后插图 [img:D:\pics\test.jpg]，保存草稿」
- [ ] 6.9 Agent 按标记位置调用插图工具，图片出现在正文对应位置，非视觉模型也能插图

## 阶段七：中断与边界

- [ ] 7.1 任务进行中按 esc → 立即中断，出现中断提示；随后可正常提交新任务
- [ ] 7.2 忙碌中输入 `/login` → 提示「⏳ 上一轮任务还在进行中，esc 可中断」，不抢占
- [ ] 7.3 空输入或纯空格回车 → 无任何输出，输入框被清空
- [ ] 7.4 粘贴一段超长任务（500 字以上）→ 命令条回显截断，任务正常执行
- [ ] 7.5 未配模型直接提交任务 → 提示先 `/model` 配置，不发起请求
- [ ] 7.6 输入 `/未知命令` → 按普通任务文本处理，不崩溃

## 阶段八：快捷键汇总

| 按键 | 作用 |
|---|---|
| Tab / Shift+Tab | 补全候选循环切换（输入 / 时） |
| Enter | 提交任务 / 确定候选 / 配置屏保存 |
| esc | 中断任务 / 取消登录等待 / 关闭帮助浮层 |
| ctrl+l | 清屏重绘欢迎卡 |
| ctrl+q | 退出应用 |
| PgUp / PgDn | 翻阅流水区 |
| 鼠标滚轮 | 翻阅流水区 |

- [ ] 8.1 逐一按一遍确认：esc 在空闲时按无副作用；ctrl+q 干净退出，Edge 保留

## 自动化自检（可选）

```powershell
python scripts/tui_smoke_test.py     # 快速冒烟：启动链 + /login 转圈 + esc 取消，约 30 秒
python scripts/final_regression.py   # 29 项完整回归：命令/补全/欢迎卡宽度/边界全覆盖，约 2 分钟
```

两个脚本均为无头运行，不弹浏览器窗口、不动 .env 之外的现场；回归套件会临时改写 .env，跑完按需恢复。

## 排错

| 现象 | 处理 |
|---|---|
| 启动失败提示端口 9222 被占 | 任务管理器结束残留的 msedge 进程后重跑 |
| 登录页一直转圈不成功 | 浏览器里确认已真正跳转离开登录页；esc 取消后重新 /login |
| 任务报模型接口错误 | `/model` 核对三项配置；opencode 网关已自动适配会话头 |
| 编辑器操作总失败 | 秀米改版了，按 `scripts/recon.py` 头部注释重跑踩点更新选择器 |
| 想看运行日志 | 源码运行在项目 `logs\app.log`；pip 安装在 `%USERPROFILE%\.xiumi-agent\logs\app.log` |
